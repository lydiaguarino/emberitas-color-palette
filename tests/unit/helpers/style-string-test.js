import { styleString } from 'color-palette/helpers/style-string';
import { module, test } from 'qunit';

module('Unit | Helper | style string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = styleString([42]);
  assert.ok(result);
});
